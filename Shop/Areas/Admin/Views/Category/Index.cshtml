@model Shop.Application.Categories.Queries.CategoriesViewModel;
@{
    ViewData["Title"] = "Управление категориями";
}
<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>

<table class="table  table-bordered">
    <thead>
    <tr>
        
        <th scope="col">Название</th>
        <th scope="col">Действия</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var category in Model.Categories)
    {
        <tr>

            <th>@category.Name</th>
            <td>
                <a class="btn bg-primary" onclick="confirmDeleteCategoy(@category.Id)">Удалить</a>
            </td>
        </tr>
    }

    </tbody>
</table>
<a class="btn bg-primary" asp-area="Admin" asp-controller="Category" asp-action="Create">Добавить</a>

